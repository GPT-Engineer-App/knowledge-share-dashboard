import React from 'react';

const PredefinedWorkflows = () => {
  return (
    <div className="p-6">
      <h1 className="mb-4 text-3xl font-bold">Predefined Workflows</h1>
      <p>Library of predefined workflows for users to choose from, activation button to start a workflow, backend generates a new workflow via API, simple form for users to input necessary parameters, backend customizes the workflow based on user input.</p>
    </div>
  );
};

export default PredefinedWorkflows;