import React from 'react';

const CentralizedWorkspace = () => {
  return (
    <div className="p-6">
      <h1 className="mb-4 text-3xl font-bold">Centralized Workspace and Dashboard</h1>
      <p>Allow users to upload and store files, sync resident requests with task management systems, track task status from submission to completion.</p>
    </div>
  );
};

export default CentralizedWorkspace;